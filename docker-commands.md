Initial Install of Docker
```cmd
docker-compose up --build
```

Start
```cmd
docker-compose up --watch
```

Install deps in the VueJS
```cmd
docker-compose exec frontend npm install <name of dependencies>
```