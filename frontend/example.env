### Frontend example.env
# Copy this file to frontend/.env and fill values (do NOT commit secrets).

# Prefix keys with VITE_ so Vite exposes them to client-side code.
VITE_API_BASE_URL=http://localhost:8067
VITE_API_KEY=CHANGEME
